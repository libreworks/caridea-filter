<?hh // decl

namespace Caridea\Filter;

class Arrays
{
    public static function coerce(mixed $var): array<arraykey,mixed>
    {
    }

    public static function split(string $pattern): (function(mixed): array)
    {
    }

    public static function explode(string $needle): (function(mixed): array)
    {
    }

    public static function slice(int $length): (function(mixed): array)
    {
    }

    public static function join(string $joiner): (function(mixed): string)
    {
    }
}
